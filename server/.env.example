# Example environment variables for the Elevate server
# Replace the values in your real .env file before running the server

# MongoDB connection string (required)
# Example: MONGO_URI=mongodb://localhost:27017/task-tracker
MONGO_URI=

# Port the server will listen on (optional, default 5000)
# Example: PORT=5000
PORT=
CLIENT_URL=